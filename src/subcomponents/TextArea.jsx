const TextArea = ({ className, placeholder }) => {

    return (
        <textarea
            className={className}
            placeholder={placeholder}
        >

        </textarea>
    );
};

export default TextArea;